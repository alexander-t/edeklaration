var assert = require('chai').assert;
var taxCalculator = require('../app/taxcalculator');

describe('A Swedish tax calculator', function() {
    it('Delete this test! It just shows that a dummy function is called.', function () {
        assert.equal(taxCalculator.ping(), "pong");
    });
});